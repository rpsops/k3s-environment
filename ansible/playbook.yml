---
# Runs inside the k3s-toolbox container (docker exec k3s-toolbox ansible-playbook ...)
# k3s startup, DNS setup, and CA trust are handled by the Makefile.
- name: Deploy cluster infrastructure via Flux
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  roles:
    - flux
