apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: registry-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: default
  source:
    repoURL: http://git-server.git-server.svc.cluster.local/infra
    targetRevision: main
    path: infrastructure/registry
  destination:
    server: https://kubernetes.default.svc
    namespace: registry
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
